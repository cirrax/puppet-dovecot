---
version: 5

defaults:
  datadir: 'data'
  data_hash: 'yaml_data'

hierarchy:
  - name: 'module dovecot, os-family'
    path: '%{facts.os.family}-family.yaml'

  - name: 'module dovecot, common'
    path: 'common.yaml'
